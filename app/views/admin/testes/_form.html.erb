<%= form_with(model: admin_testis) do |form| %>
  <% if admin_testis.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(admin_testis.errors.count, "error") %> prohibited this admin_testis from being saved:</h2>

      <ul>
        <% admin_testis.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :nome, style: "display: block" %>
    <%= form.text_field :nome %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
